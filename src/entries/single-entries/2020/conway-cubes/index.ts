import { entryForFile } from "../../../entry";

export const conwayCubes = entryForFile(
    async ({ lines, outputCallback, resultOutputCallback }) => {
        throw Error("Not implemented");
    },
    async ({ lines, outputCallback, resultOutputCallback }) => {
        throw Error("Not implemented");
    },
    { key: "conway-cubes", title: "Conway Cubes"}
);